{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "hugo local serve",
            "type": "shell",
            "command": "huge serve -D"
        },
        {
            "label": "push",
            "type": "shell",
            "command": "cf push",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}